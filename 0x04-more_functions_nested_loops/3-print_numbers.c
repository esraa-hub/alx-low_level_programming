#include "main.h"

/**
 *  print_numbers -function that checks the number sign
 *
 * Return: return 1 if 'c' is positive
 * otherwise  0 if 'c' is zero
 *  and -1 if 'c' is negative
 *
 */
void print_numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
		_putchar (i);
	_putchar ('\n');
}
